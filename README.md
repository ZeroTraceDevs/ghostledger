# ghostledger
it init git add app.ps1 README.md git commit -m "Initial commit: GhostLedger" git branch -M main git remote add origin https://github.com/ZeroTraceDevs/ghostledger.git git push -u origin main
